// Barrel for shared, cross-feature UI components.
export { default as ErrorBoundary } from "./ErrorBoundary.jsx";
// Existing primitives live in ../../components/ui.jsx and are re-exported as
// they migrate here.
